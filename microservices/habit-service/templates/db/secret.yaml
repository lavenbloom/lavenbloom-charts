apiVersion: v1
kind: Secret
metadata:
  name: habit-db-secret
  namespace: {{ .Values.db.namespace }}
type: Opaque
data:
  POSTGRES_PASSWORD: {{ .Values.db.password | b64enc | quote }}
  POSTGRES_USER: {{ .Values.db.user | b64enc | quote }}
  POSTGRES_DB: {{ .Values.db.name | b64enc | quote }}

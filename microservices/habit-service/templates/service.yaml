apiVersion: v1
kind: Service
metadata:
  name: habit-service
  namespace: {{ .Values.habitService.namespace }}
  labels:
    app: habit-service
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.habitService.port }}
      targetPort: {{ .Values.habitService.port }}
  selector:
    app: habit-service

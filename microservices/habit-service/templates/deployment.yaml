apiVersion: apps/v1
kind: Deployment
metadata:
  name: habit-service
  namespace: {{ .Values.habit-service.namespace }}
  labels:
    app: habit-service
    tier: backend
spec:
  replicas: {{ .Values.habit-service.replicas }}
  selector:
    matchLabels:
      app: habit-service
  template:
    metadata:
      labels:
        app: habit-service
        tier: backend
    spec:
      containers:
        - name: habit-service
          image: "{{ .Values.habit-service.image.repository }}:{{ .Values.habit-service.image.tag }}"
          ports:
            - containerPort: {{ .Values.habit-service.port }}
          envFrom:
            - secretRef:
                name: habit-service-secret
          resources:
            requests:
              cpu: {{ .Values.habit-service.resources.requests.cpu }}
              memory: {{ .Values.habit-service.resources.requests.memory }}
            limits:
              cpu: {{ .Values.habit-service.resources.limits.cpu }}
              memory: {{ .Values.habit-service.resources.limits.memory }}

apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: {{ .Values.notificationService.namespace }}
  labels:
    app: notification-service
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.notificationService.port }}
      targetPort: {{ .Values.notificationService.port }}
  selector:
    app: notification-service
    component: api

apiVersion: v1
kind: Service
metadata:
  name: journal-service
  namespace: {{ .Values.journal.namespace }}
  labels:
    app: journal-service
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.journal.port }}
      targetPort: {{ .Values.journal.port }}
  selector:
    app: journal-service

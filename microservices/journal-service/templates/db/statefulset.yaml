apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: journal-db
  namespace: {{ .Values.db.namespace }}
  labels:
    app: journal-db
spec:
  serviceName: journal-db-headless
  replicas: {{ .Values.db.replicas }}
  selector:
    matchLabels:
      app: journal-db
  template:
    metadata:
      labels:
        app: journal-db
    spec:
      containers:
        - name: postgres
          image: {{ .Values.db.image }}
          ports:
            - containerPort: {{ .Values.db.port }}
          envFrom:
            - secretRef:
                name: journal-db-secret
          volumeMounts:
            - name: journal-db-data
              mountPath: /var/lib/postgresql/data
              subPath: pgdata
  volumeClaimTemplates:
    - metadata:
        name: journal-db-data
      spec:
        accessModes:
          - ReadWriteOnce
        storageClassName: {{ .Values.global.storageClass }}
        resources:
          requests:
            storage: {{ .Values.db.storage.size }}

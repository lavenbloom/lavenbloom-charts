apiVersion: v1
kind: Service
metadata:
  name: journal-db
  namespace: {{ .Values.db.namespace }}
  labels:
    app: journal-db
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.db.port }}
      targetPort: {{ .Values.db.port }}
  selector:
    app: journal-db
---
apiVersion: v1
kind: Service
metadata:
  name: journal-db-headless
  namespace: {{ .Values.db.namespace }}
  labels:
    app: journal-db
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - port: {{ .Values.db.port }}
      targetPort: {{ .Values.db.port }}
  selector:
    app: journal-db

apiVersion: apps/v1
kind: Deployment
metadata:
  name: journal-service
  namespace: {{ .Values.journal-service.namespace }}
  labels:
    app: journal-service
    tier: backend
spec:
  replicas: {{ .Values.journal-service.replicas }}
  selector:
    matchLabels:
      app: journal-service
  template:
    metadata:
      labels:
        app: journal-service
        tier: backend
    spec:
      containers:
        - name: journal-service
          image: "{{ .Values.journal-service.image.repository }}:{{ .Values.journal-service.image.tag }}"
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: {{ .Values.journal-service.port }}
          envFrom:
            - secretRef:
                name: journal-service-secret
          resources:
            requests:
              cpu: {{ .Values.journal-service.resources.requests.cpu }}
              memory: {{ .Values.journal-service.resources.requests.memory }}
            limits:
              cpu: {{ .Values.journal-service.resources.limits.cpu }}
              memory: {{ .Values.journal-service.resources.limits.memory }}

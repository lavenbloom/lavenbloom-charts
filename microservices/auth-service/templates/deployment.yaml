apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: {{ .Values.auth-service.namespace }}
  labels:
    app: auth-service
    tier: backend
spec:
  replicas: {{ .Values.auth-service.replicas }}
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
        tier: backend
    spec:
      containers:
        - name: auth-service
          image: "{{ .Values.auth-service.image.repository }}:{{ .Values.auth-service.image.tag }}"
          ports:
            - containerPort: {{ .Values.auth-service.port }}
          envFrom:
            - secretRef:
                name: auth-service-secret
          resources:
            requests:
              cpu: {{ .Values.auth-service.resources.requests.cpu }}
              memory: {{ .Values.auth-service.resources.requests.memory }}
            limits:
              cpu: {{ .Values.auth-service.resources.limits.cpu }}
              memory: {{ .Values.auth-service.resources.limits.memory }}

apiVersion: v1
kind: Service
metadata:
  name: auth-db
  namespace: {{ .Values.db.namespace }}
  labels:
    app: auth-db
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.db.port }}
      targetPort: {{ .Values.db.port }}
  selector:
    app: auth-db
---
apiVersion: v1
kind: Service
metadata:
  name: auth-db-headless
  namespace: {{ .Values.db.namespace }}
  labels:
    app: auth-db
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - port: {{ .Values.db.port }}
      targetPort: {{ .Values.db.port }}
  selector:
    app: auth-db

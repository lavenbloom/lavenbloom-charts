apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: {{ .Values.auth.namespace }}
  labels:
    app: auth-service
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.auth.port }}
      targetPort: {{ .Values.auth.port }}
  selector:
    app: auth-service
